import {UNSTABLE_Disclosure as RACDisclosure, DisclosureProps} from 'react-aria-components';
import './Disclosure.css';

export function Disclosure(props: DisclosureProps) {
  return <RACDisclosure {...props} />;
}
